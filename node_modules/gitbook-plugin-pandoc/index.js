var trans = require("./transformer.js");
module.exports = {
    hooks:{},
    filters:{},
    blocks:{
        pandoc: {
        process: function(block) {
            var ret = js_toHTML(block.body);
            //ret = ret.replace(/(\r\n|\r|\n)/g,'');
            //page.content = ret;
            console.log(ret);
            return ret;
        }
        }
    }
}
